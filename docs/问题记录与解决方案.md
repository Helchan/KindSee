# KindEdit 问题记录与解决方案

本文档用于记录 KindEdit 开发和验证过程中遇到的问题、原因、解决方案和后续预防措施。凡是经过多次试错才解决的问题，或者后续容易再次踩坑的问题，都应记录到这里。

新增记录时推荐格式：

```text
## 问题标题

时间：
影响范围：
现象：
原因：
解决方案：
预防措施：
关联文件：
验证方式：
```

## 1. macOS 普通 Python 可能无法正常启动 Tk GUI

时间：初始开发阶段

影响范围：macOS 双击启动、GUI 冒烟测试

现象：

- 直接使用某些命令行 Python 启动 Tk 程序时，窗口可能无法正常弹出。
- macOS 对 Tk GUI 程序更推荐使用 `Python.app` 入口。

原因：

- macOS 上 Tk GUI 与普通命令行 Python 入口存在兼容差异。
- Homebrew 或官方 Python 安装器通常会提供 `Resources/Python.app/Contents/MacOS/Python`。

解决方案：

- `run_macos.command` 中优先查找 Python.app 入口。
- 找不到 Python.app 时再尝试 `python3.12`、`python3`。
- 启动失败时写入 `logs/macos-launch.log`。

预防措施：

- 后续不要把 macOS 启动脚本简化为只执行 `python3 kindedit.py`。
- 修改启动脚本后必须重新做 macOS GUI 冒烟验证。

关联文件：

- `run_macos.command`

验证方式：

```bash
./run_macos.command
```

## 2. macOS 自动读取窗口标题可能被辅助访问权限拦截

时间：初始 GUI 冒烟验证阶段

影响范围：自动化验证

现象：

- 使用 `osascript` 读取 Python 窗口标题时出现权限错误。
- 错误类似：`osascript 不允许辅助访问`。

原因：

- macOS 对 System Events 操作窗口信息需要辅助访问权限。
- 当前环境未授予相关权限。

解决方案：

- 不把窗口标题读取作为唯一 GUI 验证依据。
- 可通过启动脚本日志确认 Python.app 入口被调用。
- 必要时人工观察窗口是否弹出。

预防措施：

- 后续自动化验证不要默认依赖 macOS 辅助访问权限。
- 如需做完整 UI 自动化，应先确认权限或使用更适合的 GUI 测试工具。

关联文件：

- `run_macos.command`

验证方式：

```bash
tail -80 logs/macos-launch.log
```

## 3. 鼠标滚轮 delta 在 macOS 和 Windows 上不完全一致

时间：初始开发阶段

影响范围：文本编辑器滚动、树视图滚动、字体缩放

现象：

- 早期实现中直接使用 `int(event.delta / 120)`。
- 在部分 macOS 环境下，滚轮 delta 不一定稳定为 120 的倍数，可能导致滚动不灵敏或不滚动。

原因：

- Tk 在不同平台上返回的 `event.delta` 语义存在差异。

解决方案：

- 改为根据正负号判断滚动方向：

```python
units = -1 if event.delta > 0 else 1
```

预防措施：

- 后续涉及滚轮事件时，不要假设所有平台都是 120 的固定步长。
- 字体缩放、横向滚动、纵向滚动都应统一处理方向。

关联文件：

- `kindedit_app/widgets/common.py`
- `kindedit_app/widgets/tree_canvas.py`

验证方式：

- 文本区滚轮上下滚动。
- 树视图滚轮上下滚动。
- `Ctrl + 鼠标滚轮` 调整字体大小。
- `Shift + 鼠标滚轮` 横向滚动树视图。

## 4. 单文件实现导致 JSON 逻辑与应用控制器强耦合

时间：架构重构阶段

影响范围：后续支持 TXT、JS、Java 等文件类型

现象：

- 原始 `kindedit.py` 超过 1500 行。
- 配置、主题、JSON 解析、树构建、位置索引、UI 控件、页签、文件操作都混在一个文件中。
- 如果新增文件类型，容易在主窗口类中继续堆 `if/else`。

原因：

- 初始实现以快速满足 JSON 功能为主，没有先建立文档类型扩展边界。

解决方案：

- 拆分为 `kindedit_app/` 包。
- 抽出 `documents/base.py` 定义 `DocumentType` 和 `DocumentRegistry`。
- 把 JSON 专属逻辑迁移到 `documents/json_document.py`。
- `app.py` 只调用当前文档类型策略，不直接调用 `json.loads()`。

预防措施：

- 新增文件类型时必须新增文档类型策略，不要把具体解析逻辑写入 `app.py`。
- 如果 `app.py` 出现大量文件类型判断，说明架构开始退化。

关联文件：

- `kindedit.py`
- `kindedit_app/app.py`
- `kindedit_app/documents/base.py`
- `kindedit_app/documents/json_document.py`

验证方式：

```bash
python3.12 -B -m py_compile kindedit.py kindedit_app/*.py kindedit_app/documents/*.py kindedit_app/widgets/*.py
python3.12 -B -c "from kindedit_app.documents import default_registry; doc=default_registry().get('json'); result=doc.parse('{\"a\":1}'); assert not result.error; print('ok')"
```

## 5. 清空文本时状态文案曾写死为 JSON

时间：架构重构阶段

影响范围：后续多文件类型支持

现象：

- 清空文本后直接显示 `请输入 JSON 内容`。
- 对 TXT、JS、Java 等文件类型来说，该提示不一定合适。

原因：

- 应用层仍残留 JSON 专属文案。

解决方案：

- 清空文本后调用当前文档类型的 `parse()`。
- 由具体文档类型返回自己的空内容状态。

预防措施：

- 应用层不直接写具体文件类型的语法错误、空内容提示、格式化规则。
- 这些文案应尽量由文档类型策略返回。

关联文件：

- `kindedit_app/app.py`
- `kindedit_app/documents/json_document.py`

验证方式：

- 清空 JSON 文本后，状态栏应显示 JSON 文档类型提供的空内容提示。
- 后续新增文件类型时，应验证其空内容状态独立生效。

## 6. 主题不应由文档类型模块直接控制

时间：架构讨论阶段

影响范围：主题切换、多文件类型语法渲染

现象：

- 如果 JSON、JS、Java 等模块直接设置 Tk 控件颜色，后续主题切换会出现重复代码和不一致表现。

原因：

- 文档类型语义和 UI 渲染职责混淆。

解决方案：

- `theme.py` 只定义主题令牌。
- `widgets/` 根据主题令牌刷新控件。
- `documents/` 只返回结构、路径、语义 token，不直接操作 UI 颜色。
- `app.py` 在主题切换时统一调用控件的 `set_palette()`。

预防措施：

- 文档类型模块不得直接调用 `Text.tag_configure()` 设置颜色。
- 如需语法高亮，文档类型应返回类似 `("keyword", start, end)` 的语义结果，由通用编辑器根据主题渲染。

关联文件：

- `kindedit_app/theme.py`
- `kindedit_app/widgets/common.py`
- `kindedit_app/widgets/tree_canvas.py`
- `kindedit_app/documents/json_document.py`

验证方式：

- 切换明暗主题后，文本区、树视图、状态栏、菜单、滚动条颜色应同步变化。
- 文档类型模块中不应出现具体 UI 颜色控制。

## 7. 验证产生的临时文件需要清理

时间：开发验证阶段

影响范围：项目目录整洁度

现象：

- 运行 Python 编译或启动脚本后会产生：
  - `__pycache__/`
  - `kindedit_app/**/__pycache__/`
  - `logs/`

原因：

- Python 导入模块会生成字节码缓存。
- 启动脚本会写入日志文件。

解决方案：

- 验证完成后清理临时目录。

预防措施：

- 提交或交付前检查 `rg --files`。
- 如果未来引入版本控制，应加入 `.gitignore`。

关联文件：

- `__pycache__/`
- `kindedit_app/**/__pycache__/`
- `logs/`

验证方式：

```bash
rg --files
```

## 8. 页签使用普通 Tk 控件实现时偶发无响应或样式失控

时间：UI 优化阶段

影响范围：页签切换、关闭页签、新建页签、顶部工具栏

现象：

- 用户点击页签时偶发无法切换。
- 同一状态下点击页签关闭按钮 `×` 和新增页签 `+` 也可能无反应。
- 将页签和顶部菜单入口改为原生 `Button` 后，macOS 下仍出现明显边框，视觉不符合无边框工具栏和页签要求。

原因：

- 页签条使用 `Label` 加 `<Button-1>` 事件绑定实现点击。
- 页签切换、关闭、新建过程中会刷新并销毁重建页签控件，基于事件绑定的标签控件在复杂焦点/重绘时更容易出现点击事件未按预期触发。
- 原生 `Button` 的边框、内边距和高亮行为在不同平台主题下不可完全控制，尤其 macOS 下容易呈现传统按钮外观。
- 页签条是高频重绘区域，把每个页签拆成多个原生控件会增加控件重建和事件路由复杂度。
- 关闭当前活动页签后，如果 `active_tab_id` 仍指向已删除页签，后续新建、关闭或切换页签时会触发 `StopIteration`，表现为点击无响应。

解决方案：

- 顶部工具栏改为 `Canvas` 自绘图标入口，集中维护打开、保存、设置三个命中区域。
- 页签栏改为 `Canvas` 自绘页签、关闭区域和新增区域，集中维护命中测试。
- 点击事件只绑定在画布上，根据命中区域分派到切换、关闭、新建等动作。
- 切换、新建、关闭页签前先安全获取当前活动页签；关闭当前页签后先切换到仍存在的页签，避免活动页签 ID 指向已删除对象。
- 主题切换时重绘画布，避免原生控件边框和平台主题差异污染视觉。

预防措施：

- 页签、工具栏等需要无边框且跨平台一致的核心入口，优先使用集中式自绘和命中测试。
- 不要在页签条中为每个页签拆分多个短生命周期 Tk 控件。
- 如果新增工具栏入口，应在统一的绘制列表和命中区域列表中扩展，不要零散新增按钮。

关联文件：

- `kindedit_app/app.py`

验证方式：

- 连续点击不同页签，应能稳定切换。
- 连续点击 `×` 应能关闭页签。
- 连续点击 `+` 应能新增页签。

## 9. 右键菜单层级、同步开关和状态栏细节偏离预期

时间：右键菜单与同步显示优化阶段

影响范围：右键菜单、类型切换、文本编辑器、状态栏、顶部工具栏

现象：

- 右键菜单一级菜单直接平铺 `JSON 文件`、`TXT 文件` 等类型项，菜单层级不清晰。
- 类型项显示为完整类型名称，不如 `.json`、`.txt`、`.js` 等扩展名直观。
- 已有自动检测逻辑后，右键菜单中仍保留 `检测` 命令，造成入口冗余。
- 开启同步后，文本窗口插入光标和编辑体验异常。
- 单文本窗口类型仍可点击同步开关。
- 右键菜单切换 `同步` 后，鼠标回到文本编辑框时仍显示为指针光标，没有恢复为文本编辑光标。
- 状态栏右侧版本文字没有垂直居中。
- 顶部打开、保存图标视觉尺寸明显大于设置图标。
- 选中菜单项使用 `✓ 同步` 这种内联前缀后，选中项正文与未选中项正文无法左侧对齐。
- 鼠标放到右键菜单项上没有高亮，放到 `类型` 上没有触发二级菜单。
- 鼠标在一级菜单上移动时，右键菜单会自动消失。
- 鼠标从拥有二级菜单的 `类型` 移动到普通一级菜单项时，或从二级菜单移回一级菜单时，菜单仍会自动消失。
- 二级菜单打开后，鼠标移动到普通一级菜单项时，二级菜单没有自动关闭。
- 关闭一次右键菜单后，再次右键无法打开菜单。

原因：

- 早期自定义右键菜单只支持一层菜单，只能把类型项平铺。
- 菜单项复用了 `display_name`，没有针对右键菜单选择更简洁的扩展名展示。
- 同步切换与右键菜单弹出过程存在抢焦点行为，影响文本框焦点恢复。
- 同步功能没有按当前文档布局模式限制可用状态。
- 原生菜单关闭和同步命令执行后没有显式恢复文本控件 cursor，导致平台菜单的指针样式残留到文本编辑区域。
- 状态栏文字使用中心锚点绘制，在不同字体度量下视觉中心不稳定。
- 工具栏直接使用 emoji 图标，macOS 下不同 emoji 的实际字面尺寸不一致。
- 自定义菜单没有预留独立标记列，选中标记占用了正文列空间。
- 无边框 `Toplevel` 菜单只依赖 Canvas 的 `<Motion>` 事件；在 macOS 上某些弹出场景中移动事件不稳定，导致悬停状态和子菜单触发失效。
- 菜单增加了“指针离开后自动关闭”的定时逻辑；在无边框弹窗和多窗口菜单场景下，指针位置判断可能误判为离开菜单，从而销毁一级菜单。
- 二级菜单使用独立无边框 `Toplevel`，在一级和二级窗口之间移动时会产生复杂的进入/离开事件；如果同时在悬停变化中销毁子菜单窗口，容易连带触发父菜单异常关闭。
- 为避免根菜单误关，曾取消了悬停到普通一级项时关闭子菜单的逻辑，导致子菜单残留。
- 根菜单关闭时只在 `grab_current() is self` 条件下释放 grab，某些平台状态下判断不稳定，导致 grab 没有被释放，后续右键事件无法正常进入编辑器。

解决方案：

- 扩展 `FastContextMenu` 支持二级菜单；一级只显示 `类型`，二级显示扩展名列表。
- 移除右键菜单 `检测` 命令和对应入口代码，保留空白到非空、整段替换触发的自动检测。
- 右键类型子菜单统一使用文档类型默认扩展名展示，当前类型用 `✓` 标记。
- 右键菜单弹出时不再强制抢焦点；切换同步后把焦点还给文本编辑器。
- 文本编辑器使用平台文本光标；macOS 为 `ibeam`，Windows 和其它 Tk 平台为 `xterm`，同步切换、文本右键菜单关闭、鼠标重新进入文本框时只做低频幂等恢复。
- 单文本窗口类型下 `同步` 菜单项置灰不可用。
- 顶部打开、保存图标改为 Canvas 几何绘制，保证与设置图标尺寸一致。
- 状态栏按字体行高计算绘制 y 坐标，保证右侧文字垂直居中。
- 自定义菜单固定左侧标记列，`✓` 绘制在标记列中，正文统一从同一 x 坐标开始。
- 自定义菜单增加悬停状态字段和指针轮询；`<Motion>` 与定时检测当前鼠标位置共同驱动高亮和二级菜单展开。
- 移除右键菜单的自动关闭定时器；菜单关闭只由点击菜单项、按 Esc、打开下一次右键菜单等明确动作触发。
- 根菜单使用 Tk grab 接管菜单打开期间的点击事件，点击菜单区域外才关闭；菜单内移动只更新高亮和子菜单显示，不销毁父菜单。
- 悬停到普通一级菜单项时，只关闭当前子菜单，不关闭根菜单。
- 根菜单销毁时无条件尝试 `grab_release()`，确保后续右键菜单可以再次触发。
- 最终放弃自绘 `Toplevel + Canvas` 级联菜单，改用 Tk 原生 `Menu` 作为 `FastContextMenu` 的底层实现。
- `FastContextMenu` 只保留适配职责：主题配置、菜单项名称、二级菜单、置灰状态、checkbutton 选中标记和命令绑定。
- 原生 `Menu` 接管悬停高亮、二级菜单展开/收起、点击外部关闭、Esc 关闭和平台级菜单交互细节。

预防措施：

- 需要分类项时优先设计为级联菜单，不要把所有子项平铺到一级菜单。
- 右键菜单显示文案应服务操作效率，文件类型优先使用扩展名。
- 同步功能只能影响定位、滚动和高亮，不得改变文本编辑器的可编辑状态和焦点体验。
- 菜单命令执行后如果焦点回到文本编辑器，必须同步恢复文本编辑器鼠标光标样式。
- 自绘图标不要混用实际尺寸不可控的 emoji。
- 菜单选中、复选等状态应使用独立标记列表达，不要把状态符号拼进正文布局。
- 自绘弹出菜单不要只依赖单一鼠标移动事件；关键悬停行为应有当前指针位置校正。
- 不要用短周期自动关闭逻辑销毁右键菜单；如需点击外部关闭，应单独实现明确的外部点击检测。
- 级联菜单的生命周期必须由根菜单统一管理；悬停行为不能销毁根菜单。
- 关闭根菜单时必须释放所有临时输入抓取状态。
- 对平台交互细节复杂的基础控件，优先使用 Tk 原生控件；自绘只用于原生控件无法满足且行为边界简单的场景。

关联文件：

- `kindedit_app/app.py`
- `kindedit_app/widgets/common.py`
- `kindedit_app/widgets/status_bar.py`

验证方式：

- 文本编辑器右键菜单一级显示 `类型`，悬停后显示 `.json`、`.txt`、`.log`、`.xml`、`.java`、`.py`、`.js`。
- 右键菜单中不再出现 `检测`。
- 文本编辑器右键菜单中，JSON/XML 分栏类型可切换 `同步`，TXT/LOG/Java/Python/JavaScript 单窗口类型下 `同步` 置灰。
- 开启同步后文本编辑器仍能看到插入光标并正常输入。
- 切换 `同步` 后，鼠标位于文本编辑器时应显示文本编辑光标。
- 状态栏右侧版本文字垂直居中。
- 选中 `同步` 后，`同步` 文本应与 `复制`、`粘贴` 等菜单项左侧对齐。
- 鼠标悬停菜单项时应显示高亮，悬停 `类型` 时应显示二级类型菜单。
- 鼠标在一级菜单内移动时，菜单不应自动消失。
- 鼠标在 `类型` 与其它一级菜单项之间移动、在二级菜单与一级菜单之间移动时，根菜单都不应自动消失。
- 鼠标从 `类型` 移动到普通一级菜单项时，二级菜单应自动关闭。
- 关闭一次右键菜单后，应能再次右键打开菜单。

## 10. 结构树版本显示、连线锚点和类型图标留白问题

时间：结构树视觉细节优化阶段

影响范围：状态栏、JSON/XML 结构树

现象：

- 状态栏右下角版本显示为 `v2.1.0`，与当前规格版本不一致。
- 结构树父子节点点画线没有从父节点类型图标底部中点引出。
- 类型图标左右留白过大，尤其 `T`、`{ }`、`[ ]` 图标占位显得松散。
- 类型图标改为动态宽度后，同一层级不同节点类型的节点名称起始位置没有对齐。
- 右侧文本点击对象属性名时，左侧结构树只能定位到父节点，不能定位到对应子节点。
- `‹ ›` 字形视觉高度明显小于 `{ }`、`[ ]`，三类类型图标外框高度不统一。
- 节点图标左右内边距仍偏大，字符到边框的空白过宽。
- 节点图标两个符号之间使用完整空格，导致图标整体仍显得偏宽。
- 半空格间距和左右总 padding 仍偏宽，图标宽高比例还不够接近正方形。
- 点击左侧结构树节点时，右侧只高亮 value 或 `{`/`[`，输入光标仍停在原位置，没有移动到对应 key。
- 左右符号按文本起点绘制时，受字体字宽和取整影响，图标左右两侧留白不对称。
- 右键菜单切换同步后，鼠标回到文本编辑器区域仍可能残留为指针光标，而不是文本编辑光标。
- 单击左侧结构树节点时，右侧会先选中或高亮 value，再把输入光标移动到 key 附近；预期应直接定位输入光标，不先选中 value。
- 为了修复右键菜单后的光标残留，曾在文本控件 `<Motion>` 中反复恢复 cursor，导致鼠标移动时光标快速闪烁，并且 macOS 下文本区仍可能显示为指针。
- 第一次打开窗口时，如果鼠标已经位于文本编辑器上方，系统可能没有触发文本控件的进入事件，导致实际鼠标样式仍停留在指针；切换页签后由于重新进入文本区才恢复为文本光标。
- 文本右键菜单弹出并关闭后，实际鼠标样式可能停留在菜单使用的指针样式，即使文本控件 `cursor` 配置值仍然正确。

原因：

- 版本常量更新后没有同步回 `APP_VERSION_TEXT` 和规格说明。
- 树连线使用缩进位置估算锚点，没有基于父节点类型图标实际坐标计算。
- 类型图标使用统一固定宽度，普通字段图标和对象/数组图标都被迫占用较宽空间。
- 后续按单个图标文本动态计算宽度，虽然减少了留白，但破坏了同层节点文本对齐。
- JSON 位置索引只把值区域映射到节点；当光标落在属性名或冒号附近时，不在子节点值范围内，只能命中父对象范围。
- 图标外框高度依赖原有行内固定上下坐标，不是按统一图标高度集中计算。
- 图标宽度保留的左右 padding 仍偏大。
- 图标符号作为一个带空格的字符串绘制，无法单独控制中间空格的视觉宽度。
- 图标宽度只按符号宽度加固定 padding 计算，没有约束为接近图标高度。
- 结构树选中到文本的同步只调用高亮逻辑，没有移动 Text 控件的 `insert` 标记。
- 位置索引没有区分“高亮范围”和“光标落点”；对象属性需要把光标落点设为 key 起点，顶层节点需要落到 `{` 或 `[` 起点。
- 图标符号使用左锚点绘制，整体居中只保证数学宽度居中，不保证左右槽位视觉对称。
- 文本编辑器只在鼠标进入和同步切换后恢复 `cursor`，菜单命令执行后或鼠标在文本区移动过程中仍可能被平台菜单状态影响。
- 树节点选中复用了“高亮范围并移动光标”的组合方法，导致树到文本定位无法做到只移动输入光标。
- 对象属性光标落点记录在 key 开始双引号位置，根节点落点记录在 `{` 或 `[` 起点，不符合当前直接编辑的定位预期。
- 后续修复把“恢复光标”放到 `<Motion>` 高频事件中，本质是用事件补丁掩盖 cursor 归属不清的问题；频繁 `configure(cursor=...)` 会触发平台光标重绘，造成闪烁。
- `xterm` 是 Tk 默认文本光标名，但 macOS/Aqua 下更适合显式使用 `ibeam`，否则实际视觉效果可能不符合系统原生文本编辑光标预期。
- 窗口初次映射或原生菜单关闭后，Tk 不一定重新分发文本控件的进入事件；只检查 `Text.cget("cursor")` 会误判，因为配置值正确不代表系统当前显示的鼠标样式已经刷新。
- `tk_popup()` 后面的代码不应简单理解为“菜单关闭后一定执行”；原生菜单和平台事件顺序存在差异，需要允许菜单关闭后或用户回到文本区时再做一次低频刷新。

解决方案：

- 将 `APP_VERSION_TEXT` 调整为 `当前版本:v1.0.0`。
- 结构树绘制时先计算每个可见节点的类型图标位置，再根据父节点图标底部中点绘制父子连线。
- 普通值图标符号从 `T` 改为 `‹` 和 `›`。
- 类型图标宽度改为按 `{}`、`[]`、`‹›` 加半空格间距后的最大宽度统一占位，并只保留少量左右内边距。
- 三种节点类型分别使用浅色背景区分：对象浅红、数组浅黄、普通值浅绿；深浅顺序为浅红最深、浅黄次之、浅绿最淡。
- 对象、数组和普通值图标都改为左右符号分开绘制，不再依赖完整空格字符。
- JSON 对象属性建立从属性名起点到属性值结束的节点映射，让点击 key、冒号或 value 都能定位到对应子节点。
- 类型图标外框高度改为统一高度，并在行高中垂直居中绘制。
- 类型图标左右内边距进一步收窄，只保留字符到边框的少量可辨识空隙。
- 类型图标改为左右符号分开绘制，中间间距先使用半个空格宽度；统一占位宽度按符号宽度加间距计算。
- 进一步将符号间距压缩到约八分之一空格宽度，左右总 padding 压缩到约 1px。
- 图标宽度至少等于图标高度，让类型图标整体更接近正方形。
- JSON 位置索引增加 `cursor_positions`：对象属性记录 key 起点，顶层和数组元素记录值起点。
- 早期编辑器新增高亮并移动输入光标的同步方法，后续拆分为“高亮”和“只移动输入光标”两个独立能力。
- 图标左右符号改为以图标中心为基准的对称槽位绘制；左右符号分别使用 `center` 锚点放置。
- 文本编辑器新增只移动输入光标的方法，树节点单击改为只移动 `insert` 标记并清除临时同步高亮，不再先选中或高亮 value。
- 早期曾在鼠标移动、鼠标按下/释放等事件中显式恢复 `xterm` 文本编辑光标，后续废弃该高频恢复方式，改为平台文本光标和幂等恢复。
- JSON 根节点光标落点调整为顶层 `{` 或 `[` 右侧；对象属性节点光标落点调整为 key 开始双引号右侧；数组元素等没有 key 的节点仍落到值起点。
- 将文本光标名称抽取到 `platforms.text_edit_cursor_name()`：macOS 使用 `ibeam`，Windows 和其它 Tk 平台使用 `xterm`。
- 文本控件初始化时一次性设置平台文本光标；`restore_text_cursor()` 改为幂等方法，只在 cursor 被其它代码实际改掉时才重新配置。
- 移除文本控件 `<Motion>`、鼠标按下和释放中的反复 cursor 恢复逻辑，避免鼠标移动时快速闪烁。
- 新增 `request_text_cursor_refresh()`：窗口初次显示、页签切换、右键菜单弹出后只设置一次性刷新标记，并低频强制刷新一次。
- 文本控件保留 `<Enter>` 和 `<Motion>` 绑定，但只在存在刷新标记时执行一次强制刷新，随后立即清除标记；普通鼠标移动不再反复配置 cursor。
- 强制刷新时先短暂切到通用 `arrow`，再在 idle 阶段切回平台文本光标，用一次真实 cursor 变更促使 macOS/Tk 重新应用当前文本控件的鼠标样式。

预防措施：

- 修改版本号时必须同步代码常量、规格说明和状态栏验收项。
- 树结构连线不能用缩进常量硬编码，应基于实际图标几何信息绘制。
- 同一层级节点名称必须左侧对齐；图标占位可以统一，但应基于当前图标文本集合的最大宽度，而不是过宽的硬编码宽度。
- 文本 offset 到树节点 path 的映射应覆盖用户可点击的完整属性区域，不应只覆盖 value。
- 类型图标尺寸应由统一宽度、统一高度和少量内边距共同控制，不要让不同字形导致外框大小变化。
- 图标内部间距应独立控制，不要依赖普通空格字符的固定宽度。
- 类型图标需要同时约束内部间距和宽高比例，避免只收窄 padding 但仍显得过宽。
- 同步定位应分别处理高亮范围和输入光标位置，不要把二者混成同一个 offset。
- 图标内部两个符号不要用左锚点起始位置拼接，应使用中心对称的绘制方式保证左右留白一致。
- 树节点单击属于定位操作时，不应复用 value 高亮方法；需要把“文本选中/高亮”和“输入光标定位”拆成独立编辑器能力。
- 涉及原生菜单的命令回调后，如果焦点回到文本编辑器，必须在多个短延迟点恢复文本控件 cursor，抵消平台菜单状态残留。
- cursor 应有明确归属：文本控件负责自己的文本光标，工具栏、页签、状态栏只负责自身悬停光标，不能通过父窗口或高频事件反复覆盖。
- 不要在 `<Motion>` 事件中做 `configure(cursor=...)` 这类高频 UI 配置；确需恢复时应在进入控件、菜单命令结束或状态变化时做幂等恢复。
- 允许在 `<Motion>` 中做一次性 pending 检查，但不能每次移动都配置 cursor；检查和实际刷新必须通过状态标记隔离。

关联文件：

- `kindedit_app/constants.py`
- `kindedit_app/widgets/tree_canvas.py`

## 11. Markdown 预览不应复用 JSON/XML 结构树控件

时间：Markdown 支持开发阶段

影响范围：Markdown 左右分栏、文档类型扩展、同步显示入口

现象：

- Markdown 需要像 JSON 一样使用左右两个窗口，但左侧不是可展开结构树，而是渲染后的只读预览。
- 如果强行把 Markdown 预览塞进 `JsonTreeCanvas` 或把 Markdown 逻辑写入 `app.py` 的具体类型分支，会破坏文档类型扩展边界。

原因：

- 原有 `split` 模式默认含义是“左结构树 + 右文本”，不适合 Markdown 的“左预览 + 右文本”。
- Markdown 预览是 UI 渲染能力，Markdown 文档类型策略只应负责类型识别、基础校验和能力声明。

解决方案：

- 新增 `view_mode = "preview"`，由应用层按视图模式切换左侧预览面板。
- 新增 `MarkdownPreview` 通用控件承载标题、列表、引用、代码块、链接、图片占位和表格等基础预览。
- 新增 `MarkdownDocumentType` 和 `MarkdownSyntaxHighlighter`，分别接入文档类型注册表和语法渲染注册表。
- Markdown 预览分栏不使用结构树路径索引，右键菜单中的同步入口保持不可用。

预防措施：

- 新增非树形左侧视图时，应先扩展 `view_mode` 和通用控件，不要改造 JSON/XML 树控件承担无关职责。
- 文档类型模块不得直接操作 Tk 控件；预览渲染应保留在 `widgets/`。

关联文件：

- `kindedit_app/app.py`
- `kindedit_app/documents/markdown_document.py`
- `kindedit_app/syntax/markdown_highlighter.py`
- `kindedit_app/widgets/markdown_preview.py`

验证方式：

```bash
python3.12 -B -m py_compile kindedit.py kindedit_app/*.py kindedit_app/documents/*.py kindedit_app/syntax/*.py kindedit_app/widgets/*.py
python3.12 -B -c "from kindedit_app.documents import default_registry; registry=default_registry(); assert registry.detect_content('# 标题\\n\\n- item').type_id == 'markdown'; doc=registry.get('markdown'); assert not doc.parse('```python\\nprint(1)\\n```').error; assert doc.parse('```python\\nprint(1)').error; print('ok')"
```

- `kindedit_app/widgets/common.py`
- `kindedit_app/documents/json_document.py`
- `kindedit_app/platforms.py`
- `kindedit_app/app.py`
- `kindedit_app/theme.py`
- `docs/KindEdit-v1.0.0-软件开发规格说明.md`

验证方式：

- 状态栏右侧显示 `当前版本:v1.0.0`。
- 父子节点点画线从父节点类型图标底部中点引出。
- 普通值图标显示为 `‹` 和 `›`，中间使用半空格间距。
- 同一层级中对象、数组、普通值节点的名称起始位置对齐，图标左右留白明显收窄。
- 右侧文本点击属性名、冒号或属性值时，左侧结构树应定位到对应子节点。
- `{ }`、`[ ]`、`‹ ›` 三类图标外框高度一致。
- 图标左右两侧只保留很小空白。
- `{}`、`[]`、`‹›` 的两个符号之间不再使用完整空格字符。
- `{}`、`[]`、`‹›` 的两个符号之间只保留约八分之一空格宽度，图标整体接近正方形。
- 点击左侧对象属性节点时，右侧输入光标移动到该属性 key 起点；点击顶层对象或数组时，输入光标移动到顶层 `{` 或 `[` 起点。
- 三类节点图标左右留白视觉对称。
- 点击左侧对象属性节点时，右侧不再先高亮 value，输入光标直接移动到属性 key 开始双引号右侧。
- 点击左侧根节点时，右侧输入光标直接移动到顶层 `{` 或 `[` 右侧。
- 右键菜单切换同步后，鼠标位于文本编辑器区域时仍显示文本编辑光标。
- 鼠标在文本编辑器中移动时，鼠标光标不应闪烁或快速重绘。
- 第一次打开窗口后，鼠标位于文本编辑器区域时应显示文本编辑光标，不需要先切换页签。
- 文本右键菜单关闭后，鼠标回到文本编辑器区域应恢复文本编辑光标，不需要切换页签或离开窗口。

## 11. 鼠标样式使用范围不符合统一规则

时间：鼠标样式规则收敛阶段

影响范围：顶部工具栏、页签栏、状态栏超链接、文本编辑器

现象：

- 鼠标悬停在页签标题、页签关闭按钮、页签新增按钮上时显示为手形。
- 鼠标悬停在顶部打开、保存、设置图标按钮上时显示为手形。
- 预期只有超链接显示手形，文本编辑器显示文本编辑光标，其它区域都显示普通指针。

原因：

- 早期把“可点击区域”和“超链接区域”混为一类，工具栏和页签命中区也使用了 `hand2`。
- 鼠标样式规则没有写入开发原则，导致不同控件各自设置 cursor。

解决方案：

- 移除顶部工具栏命中区的 `hand2` 设置，悬停时保持普通指针。
- 移除页签栏命中区的 `hand2` 设置，悬停时保持普通指针。
- 状态栏仅保留 `获取最新版本` 超链接命中区使用 `hand2`。
- 在开发原则和规格说明中明确鼠标样式优先级：超链接手形、文本编辑器文本光标、其它区域普通指针。

预防措施：

- 新增控件时不得把“可点击”直接等同于“手形光标”。
- 只有具备网页式跳转语义的超链接才能使用手形光标。
- 文本编辑光标只能由文本编辑控件维护，其它控件不得借用或覆盖。

关联文件：

- `kindedit_app/app.py`
- `kindedit_app/widgets/status_bar.py`
- `docs/开发原则与设计模式.md`
- `docs/KindEdit-v1.0.0-软件开发规格说明.md`

验证方式：

- 鼠标悬停顶部打开、保存、设置按钮时显示普通指针。
- 鼠标悬停页签标题、关闭按钮、加号按钮时显示普通指针。
- 鼠标悬停状态栏 `获取最新版本` 时显示手形。
- 鼠标位于文本编辑器时显示文本编辑光标。

## 12. macOS 下追加选中相同内容高亮失效

时间：相同内容多查询高亮修复阶段

影响范围：文本编辑器、相同内容高亮、macOS 交互

现象：

- Windows/Linux 预期按住 Ctrl 选中文本可以把当前选中内容追加到已有相同内容高亮查询组。
- macOS 下按住 Ctrl 无法稳定进行追加选择。
- macOS 下按住 Command 拖拽可以临时选中另一段文本，但松开鼠标后，前面选中的内容会变成非选中状态，追加高亮没有按预期保留。
- macOS 下按住 Command 双击选中词时，选中的词没有稳定追加到已有相同内容高亮查询组。
- 追加选择后松开 Command/Ctrl，已有多个查询词的高亮会退化为只保留最后选中的内容。
- 进一步测试发现，松开 Command/Ctrl 时如果 Tk 仍保留最后一次真实选区，旧逻辑会把这次修饰键释放当作普通选区变化，导致查询组被最后一个选中文本替换。

原因：

- 旧实现只通过 `event.state & 0x0004` 判断 Ctrl，没有把 macOS 的 Command 修饰键纳入追加高亮触发条件。
- 旧实现为了防抖，在鼠标释放 120ms 后才读取 `sel.first` 和 `sel.last`；macOS/Tk 可能在这段时间内替换或清理当前选区，导致本次选中文本丢失。
- 相同内容高亮本质上不应依赖 Tk 保留多个真实选区，而应把每次选中的文本保存为高亮查询词。
- 松开 Command/Ctrl 会触发 `<KeyRelease>`，旧逻辑在没有当前选区时会清空 `occurrence_queries`，导致已经追加的查询词被删除。
- Command/Ctrl + 双击会先经过一次单击释放；如果这次释放暂时没有形成选区，旧逻辑可能先清空查询组，随后双击选中的词只能作为新的最后一组存在。
- 前一次修复只覆盖了“修饰键释放时没有选区”的情况，没有识别“释放的是修饰键本身”。当释放修饰键时仍存在最后选区，代码仍会按普通选择路径替换整个查询组。
- `keep_query=True` 的语义不严格：虽然调用方意图是保留已有查询组，但回调仍会读取当前 Tk 选区；当最后一次真实选区仍存在时，保留路径又退回成了普通替换路径。

解决方案：

- 追加高亮修饰键按平台识别：Windows/Linux 使用 Ctrl，macOS 使用 Command，并兼容识别 macOS Ctrl。
- 在鼠标释放或键盘选区变化时立即读取当前选中文本，保存为待高亮查询词，再执行防抖高亮。
- 防抖回调优先使用已捕获的待高亮文本；只有没有待高亮文本时才读取当前 Tk 选区。
- 多个查询词继续使用 `occurrence_queries` 持久保存，不依赖 Tk 的真实多选区。
- `<KeyRelease>` 没有当前选区时不清空已有查询组，只保留并重绘已有高亮。
- 追加操作没有当前选区时不清空已有查询组，避免 Command/Ctrl + 双击的第一次单击释放破坏历史查询词。
- Command/Ctrl + 双击最终形成选区后，按追加高亮逻辑把双击选中的词加入查询组。
- 明确识别 Ctrl/Command/Meta/Super 等追加选择修饰键的 `<KeyRelease>`；释放这些修饰键时，无论当前是否还有最后一次真实选区，都只保留并重绘已有查询组，不执行普通选区替换。
- 收紧 `keep_query=True` 语义：保留查询组时绝不读取当前 Tk 选区，只按已有 `occurrence_queries` 重绘高亮。

预防措施：

- macOS 的 Command/Ctrl/Option 修饰键不能直接套用 Windows 的 Ctrl 判断，涉及跨平台快捷键或鼠标修饰键时必须做平台化处理。
- 需要防抖的选区功能，应在事件发生时捕获关键数据，防抖只延迟计算，不延迟读取易变化的 UI 状态。
- 不要把“真实多选区”和“多个高亮查询词”混为一谈；Tk `Text` 默认只有一个主要选择区，多查询高亮应由应用状态维护。
- 修饰键释放、空选区的追加事件、双击前置单击释放都不应被当作“用户明确要求清空高亮”的操作。
- 修饰键释放必须先按 `keysym` 判断事件性质，不能只根据当前是否有选区决定是否替换查询组。
- 表示“保留查询组”的参数必须真正隔离当前 UI 选区状态，不能在内部再读取 `sel.first/sel.last`。

关联文件：

- `kindedit_app/widgets/common.py`
- `docs/KindEdit-v1.0.0-软件开发规格说明.md`

验证方式：

- 普通选中文本时，替换已有相同内容高亮查询组。
- Windows/Linux 按住 Ctrl 选中文本时，追加到已有相同内容高亮查询组。
- macOS 按住 Command 选中文本时，追加到已有相同内容高亮查询组。
- macOS 按住 Command 双击选中文本时，追加到已有相同内容高亮查询组。
- 松开鼠标后，即使前一个真实选区不再保持选中状态，前一个查询词的相同内容高亮仍然保留。
- 松开 Command/Ctrl 后，已有多个查询词的相同内容高亮仍然保留。
- 松开 Command/Ctrl 时即使最后一次真实选区仍存在，已有多个查询词的相同内容高亮仍然保留。

## 13. 顶部工具栏图标审美和风格不统一

时间：顶部工具栏视觉优化阶段

影响范围：顶部工具栏、打开按钮、保存按钮、设置按钮

现象：

- 当前打开、保存图标使用简单几何草图，外形生硬，缺少细节和层次。
- 打开、保存图标与设置图标风格不一致，视觉重量不统一。
- 打开、保存、设置三个按钮虽然尺寸接近，但图标品质和视觉风格不一致。

原因：

- 早期为了替换 emoji，只做了最小几何图形，没有建立统一的工具栏图标风格。
- 设置图标仍使用 emoji，打开和保存使用 Canvas 几何图形，导致线条、填充、尺寸和视觉重量不一致。
- 图标缺少统一视觉盒、统一描边、高光层和语义色，导致图标显得粗糙。

解决方案：

- 打开、保存、设置三个图标统一使用 Canvas 矢量绘制。
- 统一约 24×24 视觉尺寸、统一 1px 深色描边、统一浅色高光和低饱和度语义色。
- 打开图标改为后置蓝边文件加前置黄色文件夹，强化打开文件语义。
- 保存图标改为蓝色软盘加绿色标签，强化保存语义。
- 设置图标从 emoji 改为同风格矢量齿轮，保证与打开、保存图标风格一致。
- 按参考截图进一步收敛图标样式：打开和保存使用经典小图标比例；设置使用八齿齿轮剪影，但改为与打开、保存一致的蓝色系，而不是孤立的纯黑剪影。
- 三个图标在保持按钮命中区域不变的前提下，按中心等比缩小为原视觉尺寸的三分之二，让图标尺寸减少约三分之一。
- 打开图标按参考图进一步重绘为前方黄橙色矩形文件夹叠加后方蓝边白色文档，移除此前不符合参考图的斜切底边文件夹形状。
- 最终按新的线框参考图重绘：打开、保存、设置都改为统一灰黑线框图标，移除彩色填充和高光；打开为线框文件夹，保存为线框软盘，设置为线框齿轮。

预防措施：

- 工具栏图标不得混用 emoji、系统图标和临时几何草图。
- 同一工具栏内图标必须统一视觉盒、描边、色彩饱和度和细节密度。
- 视觉调整时需要同时检查图标尺寸一致性和风格一致性，不只检查是否能点击。

关联文件：

- `kindedit_app/app.py`
- `docs/KindEdit-v1.0.0-软件开发规格说明.md`

验证方式：

- 顶部打开、保存、设置三个图标视觉尺寸一致。
- 三个图标风格一致，均为 Canvas 矢量绘制，不使用 emoji。
- 打开图标能识别为文件夹/文件语义。
- 保存图标能识别为软盘语义。
- 设置图标能识别为齿轮语义。
- 设置图标颜色风格与打开、保存保持一致。
- 顶部打开、保存、设置三个图标视觉尺寸约为原来的三分之二，按钮仍保持原有可点击区域。
- 打开图标形状应与参考图一致：前方为矩形文件夹，后方为蓝边白色文档，不应出现斜切底边。
- 顶部打开、保存、设置三个图标应与线框参考图风格一致：仅使用线条，不显示下方文字，三者视觉大小一致。

## 14. SQL 内容被 Python 自动检测误判且 SQL 校验过浅

时间：2026-05-24

影响范围：SQL 自动检测、SQL 实时语法检查、SQL 右键格式化

现象：

- 输入 `select * from abc_t where aa = 1 and bb = c;` 时曾被误判为 Python，并显示 Python `invalid syntax`。
- 输入 `select * from1 abc_t where aa = 1 and bb = c;` 时曾不报 SQL 语法错误。
- 输入 `select * from abc_t where aa = 1a and1 bb = c;` 时曾不报 SQL 语法错误。
- SQL 页签右键菜单中的 `格式化` 曾处于灰色不可用状态。

原因：

- Python 内容自动检测规则把任意 `from <标识符>` 当作 Python 特征，误伤 SQL 的 `FROM 表名`。
- 初始 SQL 文档类型只检查括号、注释和字符串闭合，没有引入成熟 SQL 解析器，常见语句结构和 WHERE 表达式覆盖不足。
- SQL 文档类型的 `supports_format` 被设置为 `False`，没有提供 SQL 格式化策略。

解决方案：

- Python 内容检测收窄为真正的 `from ... import ...`。
- SQL 文档类型增加 `SqlGlotSyntaxValidator` 第三方解析器适配组件，优先使用 `sqlglot` 做 SQL 解析和格式化；依赖缺失时降级到 `BasicSqlSyntaxValidator`，覆盖 `from1`、`1a`、`and1`、缺少运算符、缺少操作数等基础错误，避免应用无法启动。
- SQL 文档类型启用 `supports_format = True`，并提供基础 SQL 美化输出。

预防措施：

- 新增语言内容检测时，规则必须尽量使用该语言的完整特征，不要用容易与其它语言冲突的孤立关键字。
- SQL 这类非标准库可完整解析的语言，如需更接近 JSON 的解析器驱动校验，应优先引入成熟解析库，并在依赖文件、规格说明和打包说明中明确声明依赖用途、默认方言和降级行为。
- 文档类型的菜单能力声明必须和实际策略一致，支持格式化时必须实现 `format_text()`。

关联文件：

- `kindedit_app/documents/python_document.py`
- `kindedit_app/documents/sql_document.py`
- `requirements.txt`
- `docs/KindEdit-v1.0.0-软件开发规格说明.md`

验证方式：

```bash
python3.12 -B -c "from kindedit_app.documents import default_registry; doc=default_registry().get('sql'); assert not doc.parse('select * from abc_t where aa = 1 and bb = c;').error; assert doc.parse('select * from1 abc_t where aa = 1 and bb = c;').error; assert doc.parse('select * from abc_t where aa = 1a and1 bb = c;').error; assert doc.supports_format; print(doc.format_text('select * from abc_t where aa = 1 and bb = c;'))"
```

## 15. 200M 级文本打开、粘贴和编辑路径存在全量字符串瓶颈

时间：2026-05-25

影响范围：文件打开、剪贴板粘贴、页签切换、实时解析、语法渲染、自动保存、保存、复制

现象：

- 打开 200M 文件时，如果直接 `read_text()` 到 Python 字符串再一次性插入 Tk 文本控件，窗口会长时间无响应。
- 文本达到 200M 后，每次编辑如果调用 `Text.get("1.0", "end-1c")` 读取全文，会造成明显卡顿。
- JSON、XML、Markdown 或代码类文档如果继续执行全量解析和语法渲染，会阻塞 UI。
- 自动保存和保存如果一次性复制完整文本，会在大文本场景下造成卡顿和额外内存峰值。

原因：

- 旧路径把 `TabState.content` 当成文档真实状态，打开、切换、保存和解析都会围绕完整字符串工作。
- `app.py` 在文本变化、页签切换、自动保存、解析、语法渲染等路径上存在多处全量读取。
- 文本控件缺少统一的分块加载、分块插入和分块保存能力，导致应用层只能使用一次性读写。

解决方案：

- 新增 `kindedit_app/large_text.py`，集中定义大文本阈值、分块大小、时间片预算和状态文案。
- `LineNumberText` 增加分块打开、分块粘贴、分块保存、字符计数、选区大小检测和大文本标记。
- `TabState` 增加 `large_content`、`content_loaded`、`large_source_path`、`content_size`，大文本页签只保存来源路径和元数据，不再把完整内容写入配置。
- `app.py` 增加大文本保护模式：大文本强制单文本窗格，暂停全量解析、结构树、Markdown 预览和语法渲染。
- 大文本打开、粘贴、自动保存、页签切换快照和手动保存均改为分块流程，并通过状态栏显示进度。
- 大文本模式禁用全文格式化、全文压缩和未选区全文复制，选区过大时拒绝复制。

预防措施：

- 后续任何涉及文本变化、页签切换、保存、复制、解析和语法渲染的改动，都必须确认大文本路径不会调用全文 `Text.get()`。
- 大文本相关阈值必须集中在 `large_text.py`，不得在多个模块散落硬编码。
- 文档类型模块不得直接处理 Tk 分块读写；文本控件负责 UI 层分块能力，应用控制器只做调度。
- 修改大文本行为时必须同步更新软件开发规格说明。

关联文件：

- `kindedit_app/large_text.py`
- `kindedit_app/widgets/common.py`
- `kindedit_app/app.py`
- `kindedit_app/tabs.py`
- `docs/开发原则与设计模式.md`
- `docs/KindEdit-v1.0.0-软件开发规格说明.md`

验证方式：

```bash
python3.12 -B -m py_compile kindedit.py kindedit_app/*.py kindedit_app/documents/*.py kindedit_app/widgets/*.py kindedit_app/syntax/*.py
```

## 20. 列编辑、关闭确认、关于入口和产品改名

时间：2026-05-26

影响范围：文本编辑器、页签关闭、窗口关闭、顶部菜单、状态栏、配置迁移、启动和构建脚本

现象：

- 文本编辑器缺少按列批量插入和删除能力。
- 页签 `×` 或整窗关闭时，未保存页签没有逐个询问保存、不保存或取消。
- 状态栏右侧承载版本、获取最新版本和品牌信息，占用底部空间。
- 产品名从 KindEdit 之前的旧名称迁移时，需要同步标题、脚本、包名和规格说明，同时不能丢失旧配置读取能力。

原因：

- 列编辑属于所有文本类型通用能力，应放在 `LineNumberText`，不能写入具体文档类型。
- 未保存确认属于应用层页签生命周期行为，应由 `app.py` 统一处理，避免页签关闭和整窗关闭分叉。
- 版本和品牌信息不是实时状态，放在状态栏会和状态消息职责混杂。
- 改名涉及代码标识、入口文件、包目录、脚本和文档，必须一次性同步，否则容易出现导入路径和规格偏差。

解决方案：

- `LineNumberText` 增加矩形列编辑状态，Windows/Linux 使用 `Alt + 鼠标拖动`，macOS 使用 `Option + 鼠标拖动`；普通字符、`BackSpace`、`Delete` 作用到每一行同列位置，短行自动补空格。
- `app.py` 增加 `_confirm_discard_or_save_tab()` 和 `_confirm_all_dirty_tabs()`，页签关闭和整窗关闭共用保存确认流程。
- 顶部菜单新增 `关于` 按钮，弹窗显示当前版本、获取最新版本地址和品牌信息；状态栏缩小到 21px，仅显示左侧状态/错误信息。
- 入口文件改为 `kindedit.py`，包目录改为 `kindedit_app/`，应用类改为 `KindEditApp`；配置目录写入 `KindEdit`，读取时兼容上一应用名和 `JsonView` 旧目录。

预防措施：

- 通用编辑器交互优先放在 `widgets/common.py`，页签生命周期行为集中在 `app.py`。
- 状态栏只承载状态，不再放固定产品信息；产品信息统一从常量通过关于弹窗展示。
- 产品改名时必须同步入口、包名、构建脚本、规格说明和问题记录，并保留旧配置迁移路径。

关联文件：

- `kindedit.py`
- `kindedit_app/app.py`
- `kindedit_app/widgets/common.py`
- `kindedit_app/widgets/status_bar.py`
- `kindedit_app/constants.py`
- `kindedit_app/config.py`
- `build_macos.command`
- `build_windows.bat`
- `run_macos.command`
- `run_windows.bat`
- `docs/KindEdit-v1.0.0-软件开发规格说明.md`

验证方式：

```bash
python3.12 -B -m py_compile kindedit.py kindedit_app/*.py kindedit_app/documents/*.py kindedit_app/widgets/*.py kindedit_app/syntax/*.py
python3.12 -B -c "from kindedit_app import KindEditApp; from kindedit_app.constants import APP_TITLE; assert APP_TITLE == 'KindEdit'; print(KindEditApp.__name__)"
```

## 15.1 多平台 Tk 细节应集中在通用控件或平台适配层

时间：2026-05-25

影响范围：页签命名、文本选中高亮、右键菜单、设置窗口标题栏、树视图连接线、文件打开保存

现象：

- 会话恢复后已有 `未命名1` 时，继续新建页签可能再次创建 `未命名1`。
- Windows 下按住 Ctrl 通过拖动或双击追加相同内容高亮不稳定。
- 右键菜单禁用项不够灰，鼠标悬停禁用项仍可能出现高亮。
- 设置窗口切换暗色主题后标题栏没有跟随，而主窗口标题栏正常。
- 树视图连接线可能穿进 `+`/`−` 方框。
- 打开文件对话框默认筛选过窄，保存未保存页签时默认扩展名可能与当前文档类型不一致。

原因：

- 未命名页签计数器只在当前进程递增，没有根据恢复出来的页签标题重新校准。
- Tk 在不同平台上的修饰键和鼠标释放事件组合存在差异。
- 原生 `Menu` 的禁用态视觉和活动项行为需要由统一适配层补充约束。
- 设置窗口是独立 `Toplevel`，不会自动继承主窗口标题栏主题适配。
- 树连接线按父图标中心绘制时，某些层级的竖线正好经过子节点折叠按钮区域。
- 文件对话框顺序和保存扩展名没有完全以当前文档类型能力为中心。

解决方案：

- 新增页签时扫描当前标题，选择下一个未占用的 `未命名<N>`。
- 在 `LineNumberText` 中显式跟踪 Ctrl 修饰键并绑定 Ctrl 鼠标释放路径，普通滚轮每次滚动 2 个文本单位。
- 在 `FastContextMenu` 中统一配置禁用文字颜色，并在禁用项悬停时清除活动项。
- 设置窗口 `apply_palette()` 同步调用标题栏主题适配。
- 树视图绘制连接线时绕开可展开节点的折叠按钮方框。
- `DocumentRegistry` 提供打开用“所有文本文件”筛选和保存用当前类型优先筛选，保存无扩展名路径时补齐当前类型默认扩展名。

预防措施：

- 这类跨平台 Tk 行为优先沉到 `widgets/` 或 `platforms.py`，不要在业务流程里堆平台分支。
- 文件类型相关默认行为优先通过 `DocumentRegistry` 和 `DocumentType` 字段表达。

关联文件：

- `kindedit_app/app.py`
- `kindedit_app/documents/base.py`
- `kindedit_app/widgets/common.py`
- `kindedit_app/widgets/tree_canvas.py`
- `kindedit_app/widgets/settings_dialog.py`
- `kindedit_app/theme.py`

验证方式：

```bash
python3.12 -B -m py_compile kindedit.py kindedit_app/*.py kindedit_app/documents/*.py kindedit_app/widgets/*.py kindedit_app/syntax/*.py
```

## 16. 树视图右键菜单误显示文本编辑器专属项

时间：2026-05-25

影响范围：树视图右键菜单、文本编辑器右键菜单、类型切换、同步开关

现象：

- 左侧结构树视图右键菜单中出现了 `类型` 和 `同步`。
- 这两个入口属于右侧文本编辑器上下文，不应出现在左侧视图窗口。

原因：

- 右键菜单优化时把类型切换和同步开关同时加入文本编辑器菜单与树视图菜单。
- 规格说明仍有把文本编辑器菜单与树视图菜单混写的旧表述，容易误导后续实现。

解决方案：

- 树视图右键菜单只保留展开、折叠、复制、复制值、复制路径等结构视图操作。
- `类型` 二级菜单和 `同步` 开关只保留在文本编辑器右键菜单中。
- 同步更新规格说明和开发原则中的右键菜单归属描述。

预防措施：

- 文本内容编辑、文档类型切换和同步开关入口应归属文本编辑器上下文菜单。
- 树视图上下文菜单只放与结构节点直接相关的操作，避免混入右侧文本编辑器专属功能。

关联文件：

- `kindedit_app/app.py`
- `docs/KindEdit-v1.0.0-软件开发规格说明.md`
- `docs/开发原则与设计模式.md`

验证方式：

```bash
python3.12 -B -m py_compile kindedit.py kindedit_app/*.py kindedit_app/documents/*.py kindedit_app/widgets/*.py kindedit_app/syntax/*.py
```

## 17. 文件拖拽只注册根窗口且工具栏悬停反馈不足

时间：2026-05-25

影响范围：顶部工具栏、页签字体、文件拖拽打开

现象：

- 页签标题字体相对文本编辑框偏大，没有随文本编辑框字号保持固定关系。
- 顶部菜单图标上下空白偏高，悬停在图标按钮上没有明确高亮和边框反馈。
- 拖拽文件到窗口内部常见区域时没有打开文件。
- 拖拽入口没有明确限制文本文件，存在误尝试打开二进制文件的风险。
- 本机 Python/Tk 环境没有内置 `tkdnd` 包，单靠 `package require tkdnd` 无法启用文件拖放。
- 拖拽文件到窗口时系统显示绿色 `+`，但松开后没有打开文件。
- 拖拽某些 `.txt` 文件时提示已跳过 1 个非文本文件。
- 对“文本文件”的理解容易被误收窄为 `.txt`，但应用已支持的 Java、Python、JavaScript、Markdown、JSON、XML、SQL、LOG 等文档类型也应允许拖拽打开。
- 对“文本文件”的理解仍不应被限制为程序已注册的扩展名；任意扩展名或无扩展名文件，只要内容是文本，也应允许拖拽打开。

原因：

- 页签字体使用固定字号，没有基于 `text_font_size` 派生。
- 工具栏 Canvas 只绘制图标和命中区，没有记录悬停项并重绘按钮背景与边框。
- `tkdnd` 只尝试注册根窗口；实际窗口内部被工具栏、页签栏、分栏、文本控件等子控件覆盖，拖到子控件时根窗口可能收不到 drop 事件。
- 拖拽路径直接进入通用打开流程，没有在拖拽入口先做文本文件过滤。
- 普通 `tk.Tk()` 不会自动加载第三方 TkDND 运行库；即使应用代码绑定了 `<<Drop>>`，没有底层 TkDND 扩展也不会收到系统文件拖放事件。
- 为兼容 Tcl/Tk 9 改用普通 `tk.Tk()` 后，仍使用普通 `widget.bind("<<Drop>>", ...)` 绑定 drop 事件；普通 Tkinter 事件对象不会自动包含 TkDND 的 `%D` 文件路径数据，导致回调拿到空路径列表。
- 拖拽文本文件过滤只按 UTF-8 严格解码，并且遇到 NUL 字节直接判定为二进制；UTF-16 等正常文本编码会被误判为非文本。
- 曾把“文本内容可读取”和“应用已注册扩展名”混成拖拽过滤条件，导致未知扩展的文本文件被错误跳过。
- 后续按已注册扩展名过滤又把未知扩展的文本文件误排除，不符合“所有文本类型文件”的要求。

解决方案：

- 页签字体改为 `text_font_size + 1`，并在主题应用、文本字号调整和设置重置后同步刷新。
- 工具栏高度从 40 调整为 36，命中区上下留白收紧为 3px。
- 工具栏维护当前悬停图标索引，悬停时绘制主题高亮背景和边框，移出后清除悬停状态并重绘。
- `tkdnd` 可用时递归注册根窗口及其子控件为文件拖放目标，统一绑定 `<<Drop>>`。
- 新增 `tkinterdnd2` 运行依赖，并下载安装到项目本地 `vendor/`；应用启动时根据当前平台和 Tcl/Tk 版本把兼容的 TkDND 目录加入 Tk `auto_path`。
- Homebrew Python 3.12 当前使用 Tcl/Tk 9.0.1，PyPI `tkinterdnd2` 自带的 macOS arm64 TkDND 2.9.3 与 Tcl/Tk 9 不兼容；补入上游 TkDND 2.9.5 macOS Tcl/Tk 9 运行库，避免旧库半加载导致 Objective-C 类冲突。
- Drop 事件改用 TkDND 专用绑定：优先使用 `dnd_bind` 获取包含 `%D` 的事件对象；不可用时直接通过 Tcl `bind` 把 `%D` 传给 Python 注册命令。
- Drop 进入和移动事件显式返回 `copy`，保持系统拖拽反馈与最终 drop 行为一致。
- Drop 数据解析兼容 Tk list 和 `file://` URI，并在未收到文件路径时显示错误状态。
- macOS 下注册 `::tk::mac::OpenDocument`，接收系统传入的打开文档路径。
- 拖拽打开前按普通文件、常见文本编码可解码、控制字符比例较低过滤，支持 UTF-8、UTF-16 和 GB18030 等文本文件，跳过目录、二进制文件和不可读取文件。
- 小文件打开读取复用同一套文本解码逻辑，避免拖拽判定通过但后续 `read_text(encoding="utf-8")` 打开失败。
- 移除拖拽入口的扩展名白名单，只按普通文件、常见文本编码可解码、控制字符比例较低判断是否允许打开。
- 未知扩展名文本文件打开后复用现有文档类型检测规则，无法识别扩展名时使用默认文档类型。
- 跳过提示恢复为“非文本文件”，避免把未知扩展文本误描述为不支持。

预防措施：

- Canvas 自绘入口新增交互状态时，应同时维护绘制状态和命中测试状态，不要只绑定点击事件。
- 文件拖拽不能只注册顶层窗口；如果主界面由子控件铺满，应递归注册主要子控件。
- 不要依赖系统 Python 自带 `tkdnd`；应通过项目依赖或打包产物携带 TkDND 运行库。
- TkDND 的文件路径在 `%D` 中，普通 Tkinter `Event` 不保证有 `data` 字段；不能用普通 `bind` 替代 TkDND 专用事件绑定。
- 文本文件判定不能只看 UTF-8 或 NUL 字节；UTF-16 文本通常含有 NUL 字节，应先尝试常见文本编码再按内容特征判断。
- 拖拽支持范围应以内容是否为文本为准，不应跟 `DocumentRegistry.file_dialog_patterns` 绑定；注册表只负责打开后的文档类型识别和渲染策略。
- 拖拽入口应先过滤文件类型，再复用通用打开流程。

关联文件：

- `kindedit_app/app.py`
- `requirements.txt`
- `vendor/tkinterdnd2/`
- `docs/KindEdit-v1.0.0-软件开发规格说明.md`

验证方式：

```bash
python3.12 -B -m py_compile kindedit.py kindedit_app/*.py kindedit_app/documents/*.py kindedit_app/widgets/*.py kindedit_app/syntax/*.py
```

## 18. 暗色标题栏、设置按钮、树连接线和字体默认值回归

时间：2026-05-25

影响范围：暗色主题、设置窗口、树形视图、文本滚动、默认字体、页签标题显示

现象：

- macOS 暗色主题下主窗口标题栏和设置窗口标题栏没有稳定跟随暗色外观。
- 设置窗口使用原生按钮时，macOS 可能忽略按钮背景色，导致暗色主题下按钮发白且文字对比度不足。
- JSON 左侧树视图中，后绘制的连接线仍可能穿进已经绘制的 `+`/`−` 方框。
- 文本编辑器鼠标滚轮滚动仍偏慢。
- 页签标题、视图窗口和文本窗口默认字体大小不符合统一 10 号要求。
- 页签标题需要按字符数而不是像素宽度固定截断。

原因：

- macOS 标题栏适配只设置了窗口 style，没有优先尝试 Tk 支持的 appearance。
- Tk 原生 `Button` 在 macOS Aqua 下不保证遵守 `bg`/`fg`。
- 树视图逐行绘制时，后续行的连接线可能覆盖之前行的展开按钮区域。
- 文本滚轮步进过小。
- 字体默认值仍保留旧的 macOS/Windows 分平台默认值，页签字体仍依赖文本字号。
- 旧页签截断按像素宽度裁剪，不符合“最多 20 个字符”的产品规则。

解决方案：

- macOS 标题栏主题适配增加 `tk::unsupported::MacWindowStyle appearance` 调用，并继续静默降级。
- macOS 明亮标题栏 appearance 必须使用 `aqua`，暗色使用 `darkaqua`；不要传 `light`，当前 Tk 会拒绝该值并导致标题栏停留在旧外观。
- 设置窗口按钮改为主题化 `Frame + Label` 控件，避免原生按钮忽略暗色背景。
- 树视图绘制改为先绘制所有连接线，再绘制展开按钮和节点内容，并按所有按钮方框分段跳过竖线。
- 文本编辑器、Markdown 预览和 JSON/XML 树视图复用统一滚轮步进，每次滚动 6 个单位。
- 视图树和文本编辑器默认字体大小统一为 10，页签字体固定 10。
- 页签标题超过 20 个字符时显示前 17 个字符加 `...`，悬停仍显示完整标题。

预防措施：

- macOS 原生控件如果不能稳定遵守主题色，应在 widget 层封装主题化控件，而不是在应用控制器里散落平台分支。
- Canvas 树连接线和节点控件应分层绘制，避免后绘制线条覆盖交互控件。
- 产品规则明确按字符数截断时，不要用像素测量替代。

关联文件：

- `kindedit_app/platforms.py`
- `kindedit_app/widgets/settings_dialog.py`
- `kindedit_app/widgets/tree_canvas.py`
- `kindedit_app/widgets/common.py`
- `kindedit_app/config.py`
- `kindedit_app/app.py`
- `docs/KindEdit-v1.0.0-软件开发规格说明.md`

验证方式：

```bash
python3.12 -B -m py_compile kindedit.py kindedit_app/*.py kindedit_app/documents/*.py kindedit_app/widgets/*.py kindedit_app/syntax/*.py
```

## 19. 设置窗口主题切换从大号开关改为单选按钮

时间：2026-05-25

影响范围：设置窗口、主题切换、明暗主题视觉一致性

现象：

- 原有“切换明暗”使用普通按钮，视觉上不像主题状态开关。
- macOS 原生按钮在不同主题下可能出现背景和文字对比不可控的问题。
- 用户要求主题切换控件与给定截图一致：胶囊轨道、白色圆形滑块，明亮主题显示“明”，暗色主题显示“暗”。
- 实现后的胶囊开关在设置窗口中占用空间过大，视觉比例不适合当前设置窗口。
- 用户最终要求放弃大号开关，改为 `主题：明 / 暗` 单选按钮形式。

原因：

- 设置窗口主题切换入口仍使用通用 `DialogButton`，没有表达当前明暗状态。
- 主题状态控件的形状、文字位置和滑块位置属于设置窗口 UI 细节，应封装在 widget 层。
- 胶囊开关适合更宽松的设置面板，但当前窗口控件密度较高，单选按钮更清晰且占用空间更小。

解决方案：

- 删除 `ThemeSwitch` Canvas 控件和相关尺寸常量。
- 在设置窗口中新增 `theme_var`，使用两个 `Radiobutton` 表示 `明` 和 `暗`。
- 选择单选按钮后直接设置 `config.theme`，调用 `app.apply_theme()` 并保存会话。
- 单选按钮显式跟随当前主题配置背景、文字、选中和 active 颜色，避免暗色主题下对比不足。

预防措施：

- 主题切换入口的 UI 状态保持在 `widgets/settings_dialog.py`，不要把控件细节放入 `app.py`。
- 设置窗口空间有限时，优先使用紧凑、平台熟悉的控件表达简单二选一状态。

关联文件：

- `kindedit_app/widgets/settings_dialog.py`
- `docs/KindEdit-v1.0.0-软件开发规格说明.md`

验证方式：

```bash
python3.12 -B -m py_compile kindedit.py kindedit_app/*.py kindedit_app/documents/*.py kindedit_app/widgets/*.py kindedit_app/syntax/*.py
```

## 21. 设置窗口主题控件压缩为单行分段选择

时间：2026-05-26

影响范围：设置窗口、主题切换

现象：

- 主题设置占用两行，`当前主题：明亮` 与 `明 / 暗` 单选按钮分离，窗口纵向空间浪费。
- 用户预期 `当前主题：` 后不显示当前值文字，末尾直接放一体式 `明 / 暗` 单选区域。
- 用户希望点击两个按钮所在矩形区域即可切换，而不是必须精确点击某个原生单选按钮。

原因：

- 原实现使用两个原生 `Radiobutton`，自然形成分散的按钮命中区和额外行布局。
- 当前值文案和可切换控件表达了同一个状态，造成重复。

解决方案：

- 新增 `ThemeSegment` Canvas 控件，统一绘制外框、左右分隔、选中背景、单选圆点和 `明` / `暗` 文案。
- 设置窗口主题行改为 `当前主题：` + 一体式分段控件同一行显示。
- 点击分段控件左半切换 `light`，点击右半切换 `dark`，并继续调用应用层主题应用和会话保存流程。

预防措施：

- 设置窗口中简单二选一状态优先封装为单个 widget，避免多个原生控件在布局、主题和命中区上各自为政。
- 状态已有可操作控件表达时，不再重复显示当前值文案。

关联文件：

- `kindedit_app/widgets/settings_dialog.py`
- `docs/KindEdit-v1.0.0-软件开发规格说明.md`

验证方式：

```bash
python3.12 -B -m py_compile kindedit.py kindedit_app/*.py kindedit_app/documents/*.py kindedit_app/widgets/*.py kindedit_app/syntax/*.py
```

## 22. 设置窗口主题控件去边框并调整重置按钮位置

时间：2026-05-26

影响范围：设置窗口、主题切换、设置按钮布局

现象：

- 一体式主题控件仍绘制了外边框、分隔线和选中底色，和用户期望的无边框单选样式不一致。
- 点击主题控件时仍按左右半区选择具体主题，而用户期望在该矩形区域内任意点击都执行明暗切换。
- `重置设置` 按钮位于主题行右侧，按钮文案过长，用户要求改为 `重置` 并放到左下角，与 `关闭` 同一行。

原因：

- `ThemeSegment` 过度模拟分段按钮，把点击区域和视觉边界都画了出来。
- 设置窗口底部按钮布局没有把操作按钮按左右两端对齐。

解决方案：

- `ThemeSegment` 不再绘制外框、分隔线和选中底色，只绘制两个单选圆点和 `明` / `暗` 文案。
- `ThemeSegment` 点击任意位置都把当前主题切换到相反状态。
- `重置设置` 改名为 `重置`，宽度与 `关闭` 一致，并移动到左下角，与 `关闭` 同一行。

预防措施：

- 如果用户描述的矩形区域是命中区，不要把它误实现成可见边框。
- 设置窗口底部主操作按钮应按左右位置表达功能分组，避免把重置类操作挤在主题项右侧。

关联文件：

- `kindedit_app/widgets/settings_dialog.py`
- `docs/KindEdit-v1.0.0-软件开发规格说明.md`

验证方式：

```bash
python3.12 -B -m py_compile kindedit.py kindedit_app/*.py kindedit_app/documents/*.py kindedit_app/widgets/*.py kindedit_app/syntax/*.py
```

## 23. macOS Option 拖动没有触发列编辑

时间：2026-05-26

影响范围：文本编辑器、列编辑、macOS 鼠标修饰键

现象：

- 在 macOS 上按住 `Option + 鼠标拖动` 没有看到列编辑选区或同列编辑效果。

原因：

- 原实现把列编辑开始、拖动和释放都绑定到固定 state mask `0x0008`。
- macOS/Tk 下 Option 可能通过 `Option`、`Alt`、`Mod1`、`Mod2` 等不同事件序列或不同 state 位上报；即使专用绑定命中，后续 `_is_column_edit_event()` 仍可能因为 state 位不匹配而拒绝进入列编辑。
- 拖动和释放阶段继续要求修饰键 state 保持一致也不稳，Tk 在鼠标拖动事件中不保证所有平台都持续带同一个修饰键位。

解决方案：

- 同时绑定 `<Alt-Button-1>`、`<Option-Button-1>`、`<Mod1-Button-1>`、`<Mod2-Button-1>`，这些专用绑定命中时强制进入列编辑。
- 普通 `<Button-1>` 路径仍保留 state mask 识别，macOS 下兼容多个可能的 Option/Alt mask。
- 一旦进入列编辑，拖动和释放只根据已有列编辑 anchor 继续更新选区，不再反复依赖修饰键 state。

预防措施：

- 跨平台鼠标修饰键不要只依赖单一 state mask；专用事件序列命中后应直接进入对应模式。
- 对拖动型交互，开始事件确认模式即可，后续 Motion/Release 应跟随模式状态完成一次交互。

关联文件：

- `kindedit_app/widgets/common.py`

验证方式：

```bash
python3.12 -B -m py_compile kindedit.py kindedit_app/*.py kindedit_app/documents/*.py kindedit_app/widgets/*.py kindedit_app/syntax/*.py
```

## 24. 列编辑垂直拖动不应选中竖线旁边字符

时间：2026-05-26

影响范围：文本编辑器、列编辑视觉反馈、列命中计算

现象：

- 在 Markdown 粗体标记 `**` 中间按住 `Option` 向下拖动时，列编辑视觉会高亮前一个或后一个 `*`。
- 用户期望从两个字符中间向下拖动时显示一条竖线，不应选中竖线前后任一字符；只有横向偏移到字符范围时才显示对应矩形选择。

原因：

- 原实现用 `Text.tag_add()` 给字符范围设置背景色来模拟列编辑选区；即便开始列和结束列相同，也至少高亮一个字符。
- 列位置直接使用 Tk 的 `@x,y` 索引，结果更接近“最近字符”，不是编辑器常见的“字符边界”模型。

解决方案：

- 新增 `_column_boundary_index()`，根据字符 bbox 的中点判断插入边界，点击两个字符之间时落在字符间的列位置。
- 同一列垂直拖动时不再给字符打 `column_selection` 背景色，而是在每一行对应列位置放置 2px 竖向 caret。
- 只有拖出横向范围时才保留矩形字符高亮；列插入仍按同一 `column_edit_column` 执行。
- 滚动、水平滚动和重绘行号时同步重绘列 caret。

预防措施：

- 列编辑的插入列和字符选区是不同概念；同列插入态应使用 caret 视觉，不要用字符背景色假装光标。
- 鼠标定位列编辑时应按字符边界计算，避免 `@x,y` 的最近字符行为让用户感觉偏前或偏后。

关联文件：

- `kindedit_app/widgets/common.py`
- `docs/KindEdit-v1.0.0-软件开发规格说明.md`

验证方式：

```bash
python3.12 -B -m py_compile kindedit.py kindedit_app/*.py kindedit_app/documents/*.py kindedit_app/widgets/*.py kindedit_app/syntax/*.py
```

## 25. 编辑器左下角滚动条交叉区域颜色不一致

时间：2026-05-26

影响范围：文本编辑器、行号栏、横向滚动条、底部视觉一致性

现象：

- 编辑器底部左侧、行号栏下方和横向滚动条左侧的交叉区域出现一小块颜色与周围不同的矩形。

原因：

- `LineNumberText` 的横向滚动条只放在文本列 `row=1,column=1`。
- 行号列 `row=1,column=0` 没有控件覆盖，露出了父容器背景，因此在底部形成一个视觉不一致的空白角块。

解决方案：

- 在 `LineNumberText` 中新增 `scroll_corner` 占位 Frame，放置在 `row=1,column=0`。
- `scroll_corner` 使用主题的 `scroll_track` 颜色，与横向滚动条轨道保持一致。

预防措施：

- 自定义滚动条布局必须显式覆盖滚动条交叉角，包括左下角和可能的右下角占位区域，避免露出父容器背景。

关联文件：

- `kindedit_app/widgets/common.py`

验证方式：

```bash
python3.12 -B -m py_compile kindedit.py kindedit_app/*.py kindedit_app/documents/*.py kindedit_app/widgets/*.py kindedit_app/syntax/*.py
```

## 26. 普通点击文本框出现双光标且 Windows 选区高亮失效

时间：2026-05-26

影响范围：文本编辑器、普通选区、相同内容高亮、列编辑、Windows/macOS 鼠标交互

现象：

- 在 Windows 文本框内普通点击后出现两条竖线：一条是左侧闪烁的 Tk 原生插入光标，另一条是右侧不闪烁的列编辑 caret。
- 双击无法稳定选中词，鼠标拖动选中后没有高亮相同内容。
- macOS 也存在拖选后相同内容高亮读取过早的风险。

原因：

- 列编辑为了兼容 `Alt/Option + 鼠标拖动`，在普通 `<Button-1>` 路径中根据 Tk event `state` mask 判断是否启动列编辑。
- 不同平台和 Tk 版本的鼠标事件 state 位不完全一致，普通点击可能被误判为列编辑，从而创建不闪烁的列编辑 caret，并干扰 Tk 文本控件的原生双击选词和拖动选区。
- 相同内容高亮在 `<ButtonRelease-1>` 中立即读取 `sel.first/sel.last`；此时 Tk 文本控件的 class binding 可能还没有完成选区更新，导致读到空选区。

解决方案：

- 普通 `<Button-1>` 只负责清理已有列编辑状态，不再根据 state mask 启动列编辑。
- 列编辑只由明确的 `<Alt-Button-1>`、`<Option-Button-1>`、`<Mod1-Button-1>`、`<Mod2-Button-1>` 专用事件启动。
- 鼠标释放后的相同内容高亮改为通过 `after_idle` 捕获选区，等待 Tk 原生选区处理完成后再进入防抖高亮。
- 列编辑状态下的鼠标释放不触发普通相同内容高亮，避免列编辑 caret 被当作普通选区处理。

预防措施：

- 普通文本选择、双击选词和拖拽选区是编辑器基础能力，不应被扩展交互通过模糊 state mask 接管。
- 跨平台修饰键鼠标交互应优先依赖明确事件序列；普通鼠标路径只能保留原生文本行为和清理状态。
- 读取 Tk 文本选区时要考虑 widget binding 与 class binding 的执行顺序，鼠标释放场景应在空闲回调中读取稳定选区。

关联文件：

- `kindedit_app/widgets/common.py`
- `docs/KindEdit-v1.0.0-软件开发规格说明.md`

验证方式：

```bash
python3.12 -B -m py_compile kindedit.py kindedit_app/*.py kindedit_app/documents/*.py kindedit_app/widgets/*.py kindedit_app/syntax/*.py
```
